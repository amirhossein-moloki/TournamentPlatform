# Dependencies
node_modules/
jspm_packages/

# Production
dist/
build/
coverage/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDEs and editors
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Optional files
# package-lock.json # If using npm and prefer not to commit it, though generally recommended
# yarn.lock # If using yarn and prefer not to commit it, though generally recommended

# Test reports
junit.xml
TEST-*.xml
*.html # for coverage reports

# Docker - if you build images locally and don't want to track them
# (though .dockerignore is the primary place for Docker build context)
# docker-compose.override.yml

# Temporary files
tmp/
temp/
*.tmp
*.bak
*.swo
*.swp

# Secrets that might be accidentally added
secrets/
*.pem
*.key

# Debugging
# Various debugger output files, e.g., from Chrome, VSCode
.profile*
.v8flags.*.json
# *.cpuprofile
# *.heapprofile
# *.heapsnapshot
# *.heaptimeline
# *.log
# *.node-heapdump

# Sequelize CLI specific (if not needed in repo)
# config/config.json # If you manage DB connection strings strictly via .env

# Yarn PnP files (if using Yarn Plug'n'Play)
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
